<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Border Color -->
    <stroke
        android:width="1dp"
        android:color="@color/netclan_color" /> <!-- Change to your desired border color -->

    <!-- Background Color (if needed) -->
    <solid android:color="@android:color/transparent" /> <!-- Transparent background, adjust as needed -->

    <!-- Optional: Corner Radius -->
    <corners android:radius="8dp" /> <!-- Adjust the radius if you want rounded corners -->
</shape>
